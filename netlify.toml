[build]
  command = "pnpm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
  NPM_FLAGS = "--legacy-peer-deps"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

git remote add origin https://github.com/ВАШ_ЮЗЕРНЕЙМ/ikak-social-network.git
git branch -M main
git push -u origin main